# Pricing

| Operation | Price | Network | Asset | Output |
|---|---|---|---|---|
| Generate SVG | $0.05 per successful call | Base mainnet (`eip155:8453`) | USDC | Inline SVG plus source |
| Generate PNG | $0.07 per successful call | Base mainnet (`eip155:8453`) | USDC | Base64 PNG plus source |

The server’s x402 challenge is authoritative if metadata and challenge values ever disagree. The atomic USDC amounts in the challenge correspond to the live configured prices.
